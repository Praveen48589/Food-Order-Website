name: hello!!

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to friends!!
        run: echo "Hello friends"
      - name: say hello to username
        run: echo "hello $USER"
        
