name: cicd

on: 
  workflow_dispatch: 

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: "clone the github repo"
        run: echo "code clone from github repo"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: "docker build"
        run: echo "build docker images"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: "test the code"
        run: echo "testing the container"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "deplot the code"
        run: echo "docker compose up"
